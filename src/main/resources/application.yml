spring:
  application:
    name: estatehub

  datasource:
    username: estatehub
    password: estatehub
    url: jdbc:mysql://localhost:3306/estatehub

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
